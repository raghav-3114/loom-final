/**
 * @file Spinner.jsx
 * @description Loading spinner indicator component for async network requests and generation states.
 */

import React from 'react';

/**
 * Spinner component placeholder.
 * Visual indicator displayed during ongoing asynchronous processing.
 */
export function Spinner() {
  return <div className="spinner" />;
}

export default Spinner;
